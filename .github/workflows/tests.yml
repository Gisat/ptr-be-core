name: tests

on:
  pull_request:
    branches:
    - main
    - master
    - dev
    - rc

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  tests-and-linting:
    uses: ./.github/workflows/code-test.yaml