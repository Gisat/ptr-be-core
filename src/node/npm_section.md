# NPM section - NodeJS code
- This code is usable only inside NodeJS based services
- Contains shared backend tools like:
    - logging
    - authorisation
    - api tools and models
    - http tools and models
    - shared backend app enums
    - shared backend models
- This code in not part of the browser build